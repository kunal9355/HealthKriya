<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="140dp"
    app:cardCornerRadius="22dp"
    app:cardElevation="10dp"
    android:foreground="?attr/selectableItemBackground"
    app:cardBackgroundColor="@color/donate_card_bg">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="16dp"
        android:orientation="vertical"
        android:gravity="center_vertical">

        <ImageView
            android:id="@+id/imgDonate"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:src="@drawable/ic_donate"
            android:contentDescription="Donate Icon"
            app:tint="@color/donate_text" />

        <TextView
            android:id="@+id/txtDonateTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Donate"
            android:textSize="18sp"
            android:textStyle="bold"
            android:textColor="@color/donate_text"
            android:layout_marginTop="10dp"/>

        <TextView
            android:id="@+id/txtDonateSubtitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Blood / Medicine"
            android:textSize="13sp"
            android:textColor="@color/donate_subtext"/>

    </LinearLayout>
</androidx.cardview.widget.CardView>
